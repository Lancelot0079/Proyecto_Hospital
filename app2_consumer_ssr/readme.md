python -m venv .venv
# Activar venv (Windows)
.venv\Scripts\activate
# o Mac/Linux
# source .venv/bin/activate
pip install -r requirements.txt
python app.py

-----
1. # entrar al directorio
cd app2_consumer_ssr
2. # crear entorno virtual
python -m venv .venv
3. # Activar venv
# Windows:
- op1   .venv\Scripts\activate
- op2   .venv\Scripts\Activate.ps1
# Mac/Linux:
source .venv/bin/activate

4. # instalar las librerías
pip install -r requirements.txt
python app.py
